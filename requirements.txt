# ==========================
# ChemGPT Gateway Dependencies
# ==========================
fastapi==0.104.1
uvicorn[standard]==0.24.0
httpx==0.25.2           # Async HTTP client to talk to microservices
pydantic==2.5.0
python-dotenv==1.0.0    # For environment variables (service URLs, secrets)
